# TEMPLATE ONLY - safe to commit, contains no real secret.
# Copy to 08-secret-db.yaml (gitignored), fill in the SAME password used
# for DJANGO_DB_PASSWORD in 02-secret-app.yaml, then:
#   kubectl apply -f k8s/08-secret-db.yaml
apiVersion: v1
kind: Secret
metadata:
  name: wger-db-secret
  namespace: wger-devops
type: Opaque
stringData:
  POSTGRES_PASSWORD: "REPLACE_ME_same_password_as_wger-app-secret"
