# TEMPLATE ONLY - safe to commit, contains no real secrets.
#
# Copy this file to 02-secret-app.yaml (which MUST be in .gitignore,
# same pattern as .env / .env.example), fill in real values, then:
#   kubectl apply -f k8s/02-secret-app.yaml
#
# Generate a real SECRET_KEY with:
#   python -c "import secrets; print(secrets.token_urlsafe(50))"
apiVersion: v1
kind: Secret
metadata:
  name: wger-app-secret
  namespace: wger-devops
type: Opaque
stringData:
  SECRET_KEY: "REPLACE_ME_with_a_long_random_string"
  DJANGO_DB_PASSWORD: "REPLACE_ME_use_the_same_password_as_in_wger-db-secret"
